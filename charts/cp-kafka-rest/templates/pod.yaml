apiVersion: v1
kind: Pod
metadata:
  name: {{ template "cp-kafka-rest.fullname" . }}
  labels:
    app: {{ template "cp-kafka-rest.name" . }}
    chart: {{ template "cp-kafka-rest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  {{- if .Values.affinity.enabled }}
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        {{- range .Values.affinity.requiredDuringSchedulingIgnoredDuringExecution.nodeSelectorTerms }}
        - matchExpressions:
          {{- range .matchExpressions }}
          - key: {{ .key | quote }}
            operator: {{ .operator }}
            values: 
            {{- range .values }}
            - {{ . | quote }}
            {{- end }}
          {{- end }}
        {{- end }}
  {{- end }}
